import { ScrollToPlayDirective } from './scroll-to-play.directive';

describe('ScrollToPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToPlayDirective();
    expect(directive).toBeTruthy();
  });
});
